import styles from './VideoViewPage.scss';
import classNames from 'classnames/bind';
const cx = classNames.bind(styles);

function VideoViewPage(props) {
  return <div className="VideoViewPage">
    영상이에용
  </div>
}

export default VideoViewPage;